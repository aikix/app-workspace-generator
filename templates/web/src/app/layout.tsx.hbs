import type { Metadata } from 'next';
import './globals.css';
import Header from '@/components/layout/Header';
import Footer from '@/components/layout/Footer';
import CookieConsent from '@/components/gdpr/CookieConsent';
import { ThemeProvider } from '@/contexts/ThemeContext';
{{#if pwa}}
import PWAProvider from '@/components/pwa/PWAProvider';
{{/if}}

export const metadata: Metadata = {
  title: '{{pascalCase projectName}}',
  description: '{{description}}',
  keywords: ['{{projectName}}', 'Next.js', 'React', 'TypeScript', 'Tailwind CSS', 'Firebase'{{#if pwa}}, 'PWA'{{/if}}],
  authors: [{ name: 'Your Name' }],
  creator: 'Your Name',
  publisher: '{{pascalCase projectName}}',
  metadataBase: new URL('https://{{projectName}}.com'),
{{#if pwa}}
  manifest: '/manifest.json',
  themeColor: '#000000',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '{{pascalCase projectName}}',
  },
  formatDetection: {
    telephone: false,
  },
{{/if}}
  openGraph: {
    type: 'website',
    locale: 'en_US',
    url: 'https://{{projectName}}.com',
    title: '{{pascalCase projectName}}',
    description: '{{description}}',
    siteName: '{{pascalCase projectName}}',
  },
  twitter: {
    card: 'summary_large_image',
    title: '{{pascalCase projectName}}',
    description: '{{description}}',
  },
{{#if pwa}}
  viewport: {
    width: 'device-width',
    initialScale: 1,
    maximumScale: 5,
    userScalable: true,
    viewportFit: 'cover',
  },
  icons: {
    icon: [
      { url: '/icons/icon-192x192.png', sizes: '192x192', type: 'image/png' },
      { url: '/icons/icon-512x512.png', sizes: '512x512', type: 'image/png' },
    ],
    apple: [
      { url: '/icons/icon-152x152.png', sizes: '152x152', type: 'image/png' },
    ],
  },
{{/if}}
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="flex flex-col min-h-screen">
        <ThemeProvider>
{{#if pwa}}
          <PWAProvider>
            <Header />
            <main className="flex-1">{children}</main>
            <Footer />
            <CookieConsent />
          </PWAProvider>
{{else}}
          <Header />
          <main className="flex-1">{children}</main>
          <Footer />
          <CookieConsent />
{{/if}}
        </ThemeProvider>
      </body>
    </html>
  );
}
